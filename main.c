#include "HeapAllocator.h"

int main(){


    //Get memory (MMAP)
    //Heap allocate
    //Heap free

    //structure for the heap metadata 
    //Structure for heap chunks

    return 0;
}

int newMalloc(size_t){

    return 0;
}

int newFree(){

    return 0;
}